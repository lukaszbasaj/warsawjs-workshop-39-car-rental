'use strict';

const ValueObject = require('./ValueObject');

class DateRange extends ValueObject {
    constructor({start,end}) {
        start = new Date(start);
        end = new Date(end);
        if(isNaNstartgetTime() || isNaN(end.getTime))
    }
}